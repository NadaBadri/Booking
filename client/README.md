# Digital Wallet UI

React (Vite) frontend for the Digital Wallet / Banking System.

## Run

```bash
npm install
npm run dev
```

Backend should be running at `http://localhost:4000` and allow cookies (`credentials: true`).

